import 'package:flutter/material.dart';

class MarkAttPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Mark Attendance Page")),
      body: Center(child: Text("This is set goal Page")),
    );
  }
}
