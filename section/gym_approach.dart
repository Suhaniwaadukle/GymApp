import 'package:flutter/material.dart';

class GymApproachPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Gym Approach")),
      body: Center(child: Text("This is Attendance Page")),
    );
  }
}
