import 'package:flutter/material.dart';

class FavFrndPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Favorite friend Page")),
      body: Center(child: Text("This is fav friend Page")),
    );
  }
}
