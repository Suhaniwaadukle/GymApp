import 'package:flutter/material.dart';

class LiveContestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Live Contest")),
      body: Center(child: Text("This is Contest Page")),
    );
  }
}
